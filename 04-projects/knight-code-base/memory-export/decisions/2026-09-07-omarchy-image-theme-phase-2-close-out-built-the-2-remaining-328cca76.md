---
id: "328cca76-0ce5-4304-b874-f50465fd733c"
type: "decision"
date: "2026-09-07"
tags: ["knight-code", "decision"]
---
# Decision: omarchy-image-theme Phase 2 close-out: built the 2 remaining CEO-plan-required states Chris confirme...

## Decision

omarchy-image-theme Phase 2 close-out: built the 2 remaining CEO-plan-required states Chris confirmed. Startup - Missing Dependency State (frame RuK3j): a blocking dependency-check card (Top Nav reduced to Brand only, since neither app mode is functional yet), a per-binary status list (wallust not found / omarchy found, showing the check is per-binary not all-or-nothing), and a Recheck action, satisfying the plan's "clear, actionable message if wallust/omarchy are missing at startup" requirement. Save - Apply Failed State (frame AFlj5): a real deep-clone of Generate - Happy Path (HEVbu) via Get+Insert, not hand-rebuilt, with the terminal Command changed to "omarchy theme set autumn-hearth", its success output line replaced by a real permission-denied failure line in $error, the other 3 flavor output lines removed since they no longer fit the omarchy-set command context, a dismissible Error Banner added ("Couldn't apply theme" / palette saved but not applied), and the cloned Save Toast deleted since a save-succeeded toast would have directly contradicted the new apply-failed state. This satisfies the plan's third mid-run-failure surface (omarchy theme set failing), distinct from the wallust generation failure already covered by Generate - Error State. Fixed the CEO plan's own self-contradiction (ceo-plans/2026-09-06-omarchy-image-theme.md, Accepted Scope, style-preset bullet): it previously said presets "only apply to image-driven generation," directly contradicted 2 lines later by the general-editor entry listing presets as available there. Reworded to state both flows offer presets with different mechanisms (wallust config pre-generation vs. a client-side blend applied directly to already-loaded values), matching what Chris confirmed is correct and what the canvas already showed. Resolved (not just noted) the self-flagged "View Results" ambiguity on the Batch Progress screen: relabeled to "Done" with a check icon, giving it an unambiguous acknowledge-and-close purpose instead of an undefined destination. Left the mid-batch collision-guard trigger as a known gap, not built: the general single-image Collision Guard modal (ps2HX) exists and is presumed reusable during a batch run per the plan's "prompts per collision" line, but no batch-specific mockup shows that trigger; carried forward to eng review rather than resolved here, per Chris's explicit allowance to carry forward self-flagged minor items.
