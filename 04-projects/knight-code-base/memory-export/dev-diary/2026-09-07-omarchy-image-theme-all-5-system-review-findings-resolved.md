---
type: "dev-diary"
date: "2026-09-07"
tags: ["knight-code", "dev-diary", "omarchy-image-theme", "design-review", "pencil"]
---
# omarchy-image-theme: all 5 system-review findings resolved

Chris resolved all 5 items raised from the full real-system review (decision cfb4ac47) against the standalone CEO plan/design review pass.  1. Picker preview.png generation: deferred to v2. Added to TODOS.md under "Omarchy Image Theme Generator" (blocked by the formal-workflow gate, application-source edits disallowed while omarchy-image-theme sits in Phase 2; decision d4a2e914 is the durable record until that edit can land, e.g. once Phase 2 closes or the workflow is paused for this maintenance edit). 2. General editor's v1 scope narrowed to simple/sparse themes only (colors.toml + backgrounds/), matching Chris's own real user themes. Full preservation/editing of a theme's other real files (icons.theme, keyboard.rgb, vscode.json, neovim.lua, preview images, chromium.theme, shell.lock.toml) deferred to v2, same TODOS.md blocker as item 1 (decision 671c335f). v1 still owes non-destructive handling when pointed at a full-featured theme, not full support. 3. mode field: settled, the app never sets it in v1 or v2, relies entirely on Omarchy's own auto-detect (decision 5d1ec5f9). 4. Collision guard: real gap, fixed now. Must check both ~/.config/omarchy/themes/ and $OMARCHY_PATH/themes/, fresh every time. Fixed in the CEO plan doc's collision-guard bullet and in the Collision Guard mockup's Detail text (node I7Ust on frame ps2HX), which previously implied a user-directory-only check (decision f03b80ae). 5. Wallpaper pinning: real requirement, not left open. Save on an already-active theme in the general editor must explicitly re-pin the current wallpaper after the omarchy theme set call, since theme apply and wallpaper cycling share one call. Added to the CEO plan doc's Foundational Decisions section. Purely backend/sequencing, confirmed no mockup change needed (decision 955470e9).  Both CEO plan doc edits (ceo-plans/2026-09-06-omarchy-image-theme.md: collision-guard bullet, new wallpaper-pinning foundational decision) and the one Pencil mockup text edit (Collision Guard Detail text) landed cleanly, outside the formal-workflow gate's reach since the plan doc lives under ~/.knightcode/ (outside the tracked repo) and the Pencil edit is an MCP call, not a filesystem Edit/Write. Only the TODOS.md edit inside the Knight Code repo itself got blocked.  Design Review status: every open item from the CEO-plan cross-check and the full system review is now resolved (accepted, deferred with a tracked reason, or fixed on canvas/in the plan doc). No further mockup changes are needed for any of the 5 items, items 1 and 2 are scope decisions with no visual surface, item 3 is an explicit non-feature, item 4 changed 2 lines of text (already done), item 5 is backend-only. Phase 2 (Design Review) is ready to close on its merits. The one real outstanding item is process, not design: the TODOS.md entries for items 1 and 2 exist only as decision-log records right now, not yet written to the file, because the formal-workflow gate itself is what's blocking that edit while Phase 2 is open.
