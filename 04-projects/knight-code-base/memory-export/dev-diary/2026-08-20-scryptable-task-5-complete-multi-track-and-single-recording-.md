---
type: "dev-diary"
date: "2026-08-20"
tags: ["knight-code", "dev-diary", "scryptable", "task-5", "code-review", "drag-and-drop", "diarization-marker"]
---
# Scryptable Task 5 complete: multi-track and single-recording import, drag-and-drop, a real diarization-marker fix from code review

Proceeded to Task 5 per Chris's own explicit go-ahead, relayed alongside the Task 4 push and Source Archives instructions this session.  Generalized New Recording Setup (previously tightly coupled to Craig's zip-plan semantics) to accept a plan_fn callable, so all three import sources share the same name field, live preview, and containment checks instead of duplicating that dialog three times, a real refactor of already-shipped Task 4 code rather than new duplication. Built recording_import.py (multi-track folder or explicit file-list plan/execute, single-recording plan/execute, no zip-slip risk since sources are always real files already on disk, Path.name already strips any directory component). Import now opens a real three-option dropdown (Discord Bot Archive/Craig, Multi-Track Audio Files, Single Recording) from the toolbar button, the File menu (previously a dead, unconnected action), matching the design record. Added drag_drop_overlay.py for the orange "established"/green "one-time" states, driven by real dragEnterEvent/dragMoveEvent/dragLeaveEvent/dropEvent handlers routing a real drop by file type.  Ran this project's standing two-axis code review (Standards axis, Spec axis) against the diff before commit. Standards axis found zero hard violations, five judgement-call smells (worst: a duplicated "Open a Workspace First" QMessageBox block), fixed the duplication with a shared _require_workspace() helper. Spec axis found two real things: (1) a multi-track folder containing exactly one audio file lands the same shape on disk as a real single-recording import, undermining the file-count signal the "routes toward diarization" acceptance criterion's own implementation was relying on, since Task 7 (not built yet) would have no reliable way to tell them apart; verified directly by constructing the exact collision case, fixed with a real, folder-level marker file (.scryptable-needs-diarization) written only by single-recording import, never by multi-track regardless of file count, keeping the signal folder-is-truth-durable rather than living only in a rebuildable state cache. (2) the drag-and-drop overlay's own locked design copy for the no-workspace state ("Drop for a one-time run") promises behavior the app can't currently deliver, since no standalone one-time-run pipeline exists until Task 6/7 are built; left explicitly flagged in the Task Breakdown and Lessons Learned rather than either rewriting locked design copy without basis or silently shipping the inconsistency unflagged.  80/80 pytest-qt tests pass (new test_recording_import.py, test_import_flow.py, plus fixes to test_new_recording_setup.py and test_craig_import_flow.py for the generalized dialog's constructor signature), plus a real, non-test launch/interaction smoke script covering a real multi-track import, a real single-recording import, the diarization-marker distinction (including the one-file-multi-track collision case, confirmed resolved), a real QDropEvent, and both overlay states. Re-ran Task 4's own smoke script afterward to confirm the Craig import path still works correctly through the refactored dialog, it does. Scanned the full diff against Knight Code's banned-language list, clean.  Committed 5593312, local only, matching this project's standing pattern; Chris's push instruction this session covered Task 4 specifically, not yet re-confirmed for Task 5. Updated in the same pass: 07 Progress/kanban.md (Task 5 moved In Progress to Done), 04 Execution/Task Breakdown.md's Task 5 status line and all three acceptance-criteria checkboxes with the marker-fix and overlay-gap notes, 04 Execution/Task Sequence.md, 00 Overview.md's Continuation Point as a full replace, 06 Lessons Learned/README.md plus a new Task 5 build notes file, and Projects/_index.md's Scryptable row.  Next real step is Task 6 (transcription pipeline integration), needing Chris's own explicit go-ahead per this project's standing per-step confirmation pattern, and a separate push instruction whenever Chris wants Task 5's commit live on GitHub.
