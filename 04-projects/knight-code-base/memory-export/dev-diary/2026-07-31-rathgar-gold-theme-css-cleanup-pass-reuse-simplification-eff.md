---
type: "dev-diary"
date: "2026-07-31"
tags: ["knight-code", "dev-diary", "obsidian", "ttrpg-rathgar-gold-theme", "cleanup", "css"]
---
# Rathgar Gold theme.css cleanup pass (reuse/simplification/efficiency/altitude)

Applied a deduped, behavior-preserving cleanup pass to ttrpg-rathgar-gold-theme/theme.css, covering the full file (not just the same-day Style Settings diff). Changes: introduced -rgb custom-property variants (--rg-accent-gold-rgb, --rg-status-alive/-deceased/-unknown/-urgent-rgb, --rg-text-muted-rgb) for both .theme-dark and .theme-light, following the existing --interactive-accent-rgb precedent, and replaced every raw literal RGB triplet that exactly matched one of these six colors (--rg-border, --background-modifier-hover/-success, --text-faint/-selection/-highlight-bg, --interactive-accent-rgb, --canvas-color/-dot-pattern, the success/example/warning/caution/quote callout colors, and all four .rg-pill-* color families) with a var() reference. Left --background-modifier-error(-rgb) and the danger/info/tip/question callout colors untouched: crimson isn't in Chris's given whitelist and info/tip/question/danger's light-mode values don't cleanly match any hex-defined token (partial/coincidental match only). Merged the duplicated crimson accent-swap block (dashboard-sr scope + card crimson-metadata scope, byte-identical declarations) into one comma-separated selector, preserving the Canvas-embed-cssclasses-don't-propagate rationale comment. Collapsed 11 dice-roller rule-group pairs (dashboard-sr / canvas-sr) using :is(), collapsed the 5 dashboard background-image selector groups using :is() the same way, verified specificity was unaffected in both cases (no competing same-property rule at a different specificity exists for either selector set). Merged the table-token, statblock-token, and nav-indentation-guide-color .theme-dark/.theme-light blocks down to their genuinely-shared properties, splitting out only the properties that truly differ per mode. Deleted a dead duplicate margin:0 block on the infobox table rule, folded border-style:dashed into the base rule for danger/caution/question callouts (deleting the trailing dashed-only rule each), merged the twice-declared dashboard-card and canvas-sr-card rules (glass look + sizing, no property overlap), and fixed 8 stray "value ;" space-before-semicolon typos left over from an earlier !important removal pass. Verified: brace-balanced, banned-language check clean, no trailing-comma-before-brace or empty rule blocks. Did not touch the @font-face blocks, the scrollbar universal-selector rules, or the :has() chain selector, all explicitly out of scope. No version bump applied -- this is an internal-only refactor with zero visual/behavioral change, unlike the same-day 1.0.3->1.1.0 Style Settings feature bump, so it doesn't meet this project's version-bump bar (a bump is for user-visible change, not silent cleanup).
