---
type: "dev-diary"
date: "2026-08-15"
tags: ["knight-code", "dev-diary", "scryptable", "formal-dev-workflow", "eng-review", "anthropic-oauth", "resolution"]
---
# Scryptable eng review item 1 (Anthropic OAuth) resolved: dropped, no new architecture needed

Chris resolved the first of eng review's two open items for Scryptable (protocol-whisper-app). Confirmed via the coordinator: drop the "Sign in with Anthropic" OAuth toggle entirely (option A of the three presented in protocol-whisper-app-eng-review.md). Anthropic becomes API-key-only, matching OpenAI and custom endpoints.  Chris also asked whether MCP tooling was a substitute mechanism for routing to local models. Clarified it is not: MCP is tool/context access for whichever model is already active, not a model-selection mechanism, so it doesn't address "run this against my own local model." He then confirmed the already-existing custom OpenAI-compatible endpoint provider (decision 070855a6) is the right mechanism for local/custom runners generically (Ollama, LM Studio, vLLM, anything else, not just a local Claude Code install), since most local runners mimic the OpenAI API shape. This closes item 1 with zero new architecture, only a UI-labeling change (the custom-endpoint option needs a clear "run it locally" label rather than reading as a generic afterthought) and one fewer auth path to build.  Logged as decision a0875546, superseding 085a69e9 (the original OAuth design). promise 54a11c2a (the combined two-item promise) fulfilled with a note; item 2 (CUDA/CPU distribution) re-logged as its own promise, 03fb101d, since it's still open and unrelated to this resolution. Updated protocol-whisper-app-eng-review.md's item 5, its test-review reference to item 5, its failure-modes note, and its Completion section to reflect the resolution, all in the same pass rather than leaving the doc saying "needs Chris's call" after the call was already made.  Not yet proceeding to Phase 3.5 (Security Review): explicit instruction to hold until item 2 also resolves, even though this skill's own default rule would normally let a lone concern ride along without blocking. protocol-whisper-app.json's phase field already reads "security" (set when eng review completed), which is accurate as a "what's next" label, not a claim that Security Review has run.
