---
type: "dev-diary"
date: "2026-08-15"
tags: ["knight-code", "dev-diary", "scryptable", "devknight", "formal-dev-workflow", "vault-currency", "process-gap"]
---
# Scryptable had no durable location: recovered from decision_log and formal-workflow state, not lost

Chris asked to resume Scryptable (formerly StemScrypt, originally "Protocol Whisper App") and DevKnight found no code repo on disk, no dev-diary entry, and no populated vault folder for it. Chris was frustrated real design work seemed to have vanished with no way to resume from cold.  It was not actually lost, it was scattered outside every place this project's standing orientation checks look:  1. 25 real decisions in decision_log (query "scryptable"/"StemScrypt"/"Protocol Whisper"), covering the full product shape: three import scenarios, WhisperX+pyannote diarization, AI Provider settings (Anthropic OAuth/API key, OpenAI, custom endpoint), Obsidian direct-write integration, MCP free/REST API paywalled-later, the OSS-tool-name UI hygiene rule, Discord-bot-deferred-to-Craig, nav architecture, "Recording" terminology, consolidated Preferences, two-tier CUDA/CPU detection, and DevKnight's own PySide6 scope expansion (b4098734). 2. Real formal-dev-workflow state at ~/.knightcode/projects/ValleytheKnight-knight-code/formal-workflow/: protocol-whisper-app.json shows phase "eng" with completedPhases ["ceo","design"], plus a full CEO-review markdown (APPROVED) and design-review markdown (APPROVED, Scope Stability Gate returned stable after real pen.dev mockup rounds and two design-critique passes). Both dated 2026-08-14, the same day as this recovery session. 3. The workflow's active feature slot was parked (active.json points at "hookselftest"; protocol-whisper-app's real active state sits at active.json.parked-by-security-task), set aside while formal-dev-workflow itself gained a Phase 3.5 Security Review step. This parking is why nothing about Scryptable surfaced as "in progress" anywhere obvious. 4. The vault's Projects/_index.md still carried a stale row under the old name "Protocol Whisper App," claiming Planning phase and "plan-ceo-review owed" as of 2026-07-30, linking to a vault file that never existed. The real Scryptable/ folder in the vault existed but was completely empty.  Root cause: this project's planning work happened entirely through decision_log calls and the formal-dev-workflow skill's own state files, neither of which DevKnight's standing "keep the vault current" triggers watch for a project with no active devknight session driving it. The five named triggers (plan review completes, decision locked, task breakdown changes, milestone scope moves, build task starts/finishes) all fired for this project today, but nothing wired formal-dev-workflow's own phase completions into the same vault-currency repair devknight already does for its own dispatches.  Fixed this session: Projects/_index.md's row corrected (right name, right phase, right date, names the parked-workflow blocker). Scryptable/00 Overview.md written with a real Continuation Point pointing at the actual state (decision_log entries, the formal-workflow json/markdown files, the parked active.json, and the concrete resume steps). No code written, no repo created yet, per Chris's explicit instruction to research and propose only.  Real gap still open, not fixed by this session: formal-dev-workflow's phase completions (CEO/design/eng/security review approvals) don't currently trigger any vault-sync step the way a devknight-dispatched plan review does. Worth a real fix later, likely a hook similar to vault-sync-nudge-hook.ts but keyed on formal-workflow state-file writes rather than git commands.
