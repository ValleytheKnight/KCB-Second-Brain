---
type: "dev-diary"
date: "2026-08-07"
tags: ["knight-code", "dev-diary", "skills", "improve-codebase-architecture", "domain-modeling", "mattpocock"]
---
# Ported improve-codebase-architecture and domain-modeling from mattpocock/skills

Chris asked for github.com/mattpocock/skills/tree/main/skills/engineering/improve-codebase-architecture installed into Knight Code. Fetched its SKILL.md, HTML-REPORT.md, and agents/openai.yaml via gh api. It depends on /codebase-design (already ported), /grilling (now grill-me after the earlier interview-me/grill-me consolidation this session), and /domain-modeling (not present).  Asked Chris two questions before building: whether to also port domain-modeling or adapt improve-codebase-architecture to skip its CONTEXT.md/ADR side effects (chose: also port domain-modeling), and whether to keep upstream's disable-model-invocation: true or make it ambient (chose: keep explicit-only, matches upstream's own default for a deliberate, heavyweight action).  Fetching domain-modeling's own SKILL.md surfaced a second decision point mid-build: its "offer an ADR" step writes docs/adr/NNNN-slug.md files, which duplicates Knight Code's own decision_log/decision_search tools. Flagged it per the project's Knight Code First rule rather than silently porting a parallel system. Chris chose to route ADRs through decision_log instead.  Built both skills under Knight Code's own template conventions: SKILL.md.tmpl with {{PREAMBLE}}, plain untemplated reference files alongside (CONTEXT-FORMAT.md, HTML-REPORT.md, matching the codebase-design/DEEPENING.md pattern), knightcode-<name> cross-references throughout instead of upstream's /slash-command style. Dropped both skills' agents/openai.yaml, Codex-specific, out of scope for a single-host Claude Code project. codebase-design's own ported glossary had already swapped one of its listed vocabulary words for "payoff" since the original term is banned AI-tell language here; matched that swap in both new skills for consistency. Regenerated via gen:skill-docs, published via skills:install, verified via skills:check (64 skills matching, 0 drift) and a skills-graph query.
