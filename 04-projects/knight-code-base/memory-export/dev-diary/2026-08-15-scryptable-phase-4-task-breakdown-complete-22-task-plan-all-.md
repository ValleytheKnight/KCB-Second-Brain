---
type: "dev-diary"
date: "2026-08-15"
tags: ["knight-code", "dev-diary", "scryptable", "formal-dev-workflow", "task-breakdown", "phase-4", "security-findings"]
---
# Scryptable Phase 4 Task Breakdown complete: 22-task plan, all security findings folded in, enforcement hook now open

Completed formal-dev-workflow's Phase 4, Task Breakdown, for protocol-whisper-app. Followed knightcode-planning-and-task-breakdown's process directly: read-only pass over the approved CEO/design/eng/security review docs and whisper_app_design.pen first (no new decisions re-litigated), mapped the real dependency graph, sliced vertically per this project's standing rule rather than horizontally.  Produced 22 tasks across 7 phases: Foundation (app shell, episode-state core, first-run setup), Import/Core Pipeline (Craig import, multi-track/single import, transcription, diarization, speaker identities), Assembly/Review/Export/Obsidian (TASMAS wrapping, corrections manager, QA review, export, Obsidian write), AI Provider/LLM Handoff (provider settings post-OAuth-removal, handoff chat, GPU upgrade paths), MCP (client, server, preferences tab), Preferences/Accessibility (consolidated 8-tab screen, the 5 accessibility gaps), and Packaging (CPU/CUDA PyInstaller variants). Every task has real acceptance criteria, a verification step, dependencies, module-level scope, and a size estimate; checkpoints sit after every 2-3 tasks per the skill's own guidance.  The real discipline point: all 4 of Phase 3.5's security findings got folded directly into the specific task each one actually belongs to, as concrete, testable acceptance criteria, rather than left as a separate security-cleanup task at the end where they'd be easy to silently drop. Zip-slip containment lives in Task 4 (Craig import) with an explicit unit-test requirement using a malicious test archive. The custom-endpoint standing rule lives in Task 14 (AI Provider Settings) as a constraint with its own test. MCP client tool-output flagging and the confirm-step requirement live in Task 17. MCP server default-off, localhost-only binding, and the shared-token requirement live in Task 18, with an explicit automated-test requirement (reject any non-127.0.0.1 bind, reject unauthenticated/wrong-token connections).  Updated protocol-whisper-app.json: phase is now "implementation", completedPhases is ["ceo","design","eng","security","planning"]. This is what formal-dev-workflow's own Phase 4 checklist requires (the "CRITICAL" instruction to open the enforcement hook on completion), not a separate decision to start building. No implementation code was written this turn. Logged decision 1586d360 with the full completion summary, and updated the vault's Scryptable/00 Overview.md and Projects/_index.md in the same pass to say plainly that the gate is open but nothing has been built, so a future session (or Chris re-reading this) can't mistake "hook open" for "building has started."  Next real step is Task 1 (app shell scaffold), the first item in the new task list. Same standing pattern as every phase transition so far: needs Chris's explicit go-ahead, not assumed from this phase's own completion.
